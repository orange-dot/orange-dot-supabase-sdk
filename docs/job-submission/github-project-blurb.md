# GitHub Project Blurb

C# Supabase SDK repo focused on idiomatic .NET integration, typed client composition, ASP.NET Core usage patterns, and runnable samples. The main branch contains the server-side .NET SDK line, while the `dev` branch also carries a Unity-first prototype package built on portable auth, PostgREST, functions, and storage modules.
